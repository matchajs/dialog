define(function(require, exports, module) {
    var $ = require('jquery');
    var Dialog = require('dialog');

    var template = require('./prompt.tpl');

    var Prompt = Dialog.extend({

    });

    module.exports = Prompt;
});